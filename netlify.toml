[build]
  command       = "npm run build"
  publish       = "."

[build.environment]
  NODE_VERSION  = "18"

# Environment variables are set in the Netlify dashboard — NOT here.
# Required: SUPABASE_URL, SUPABASE_ANON_KEY, DEFAULT_CURRENCY
